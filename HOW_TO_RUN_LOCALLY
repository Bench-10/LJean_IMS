1.FIRST: -Change the .env configuration make sure teh information match on what is in your pgadmin.

2.SECOND: -Create a database(you can make your own or just copy the name in the .env).

3.THIRD: -Run the following commands sequentially that is in the "db_tables.txt" in your database.

4.FOURTH: -Create two terminals(to run the frontend and backend separately).

5.FIFTH: -To run the backend just type "npm run dev" on the terminal.

6.SIXTH: -On the other terminal type "cd frontend" then type "npm run dev" to run the frontend.