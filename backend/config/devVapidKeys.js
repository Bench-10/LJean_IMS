// Development-only VAPID keys.
// Keep these empty in version control. You can populate them locally when
// running the backend in development mode so the push API works without
// touching production secrets.

export const DEV_VAPID_PUBLIC_KEY = '';
export const DEV_VAPID_PRIVATE_KEY = '';
